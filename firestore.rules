rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }

    match /users/{userId} {
      // Simplest possible rule for user access
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
